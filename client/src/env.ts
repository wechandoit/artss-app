export const API_URL: string = import.meta.env.VITE_FLASK_API_URL;

// import dotenv from "dotenv";

// // Specify the path to your .env file (you can use an absolute path or relative path)
// console.log("help");
// dotenv.config({ path: "../../../artssapp/.env" });

// // You can access the environment variables like this:
// console.log(process.env.FLASK_API_URL);

// export const API_URL: string = process.env.FLASK_API_URL!;

// console.log(`env: ${API_URL}`);

// export const DB_USR: string = import.meta.env.DB_USER;
// export const DB_PWD: string = import.meta.env.DB_PASSWORD;
// export const DB_HOST: string = import.meta.env.DB_HOST;
// export const DB_PORT: number = parseInt(import.meta.env.VITE_DB_PORT, 10);
// export const DB_NAME: string = import.meta.env.DB_NAME;
